# mk-website

Quelle für martinkriegler.com (Cloudflare, Auto-Deploy via GitHub).

## Änderungen live schalten (Terminal)

    cd ~/Projekte/mk-website
    git add -A
    git commit -m "Was geändert wurde"
    git push

Cloudflare baut nach dem Push automatisch neu (~2–4 Min).
